<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Extend Partner Form View -->
    <record id="res_partner_view_form_inherit_asterisk" model="ir.ui.view">
        <field name="name">res.partner.view.form.inherit.asterisk</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_make_call" type="object"
                        class="oe_stat_button" icon="fa-phone"
                        invisible="not phone">
                    <div class="o_stat_info">
                        <span class="o_stat_text">Gọi điện</span>
                    </div>
                </button>
                <button name="action_view_calls" type="object"
                        class="oe_stat_button" icon="fa-history"
                        invisible="call_count == 0">
                    <field name="call_count" widget="statinfo" string="Cuộc gọi"/>
                </button>
            </xpath>
        </field>
    </record>

    <!-- Extend Partner List View - Add phone click-to-call -->
    <record id="res_partner_view_list_inherit_asterisk" model="ir.ui.view">
        <field name="name">res.partner.view.list.inherit.asterisk</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='phone']" position="attributes">
                <attribute name="widget">phone</attribute>
            </xpath>
        </field>
    </record>
</odoo>
